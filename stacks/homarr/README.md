# Homarr — dashboard (dash.rubenlav.dev)
Editor visual drag & drop. `SECRET_ENCRYPTION_KEY` en `.env` (copia en Bitwarden):
cifra las credenciales de integraciones guardadas en `appdata/` (fuera de Git,
dentro del backup diario de Backrest).
