# Cloudflare Tunnel — Internet sin abrir puertos
Conexión saliente a Cloudflare; `TUNNEL_TOKEN` en `.env` (y en Bitwarden).
Las rutas públicas se gestionan en Zero Trust → Redes → Conectores → homelab.
Ver doc 12.
