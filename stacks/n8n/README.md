# n8n — automatizaciones (n8n.rubenlav.dev)
`N8N_ENCRYPTION_KEY` en `.env` (y en Bitwarden): cifra las credenciales
guardadas en los flujos. Workflows y BD en `data/` (fuera de Git,
dentro del backup diario de Backrest).
